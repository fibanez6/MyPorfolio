import { ComponentSingleStyleConfig } from '@chakra-ui/react';

const Link = {
  baseStyle:({colorMode})=>({
    // backgroundColor:'#8B3B8F',
    // color: colorMode === 'light' ? '#06008B' : '#ccc',
    // transition:'background-color 1s linear',
    // _hover:{
      // backgroundColor: 'red',
    // }
  }),
} as ComponentSingleStyleConfig;

export default Link;